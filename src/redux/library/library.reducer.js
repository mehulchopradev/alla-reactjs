const INITIAL_STATE = {
  books: []
};

const libraryReducer = (currentState = INITIAL_STATE, action) => {
  switch(action.type) {
    default:
      return currentState
  }
}

export default libraryReducer;